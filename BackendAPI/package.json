{
  "name": "backendapi-fastapi-shim",
  "version": "0.0.1",
  "private": true,
  "description": "Shim for preview runners that insist on npm; proxies to bash start.sh to launch FastAPI via uvicorn on port 3001.",
  "scripts": {
    "start": "bash start.sh",
    "start:dev": "bash start.sh"
  }
}
